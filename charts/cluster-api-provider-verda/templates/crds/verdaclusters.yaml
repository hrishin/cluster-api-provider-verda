# Generated from config/ by hack/sync-charts.py. Do not edit.
{{- if .Values.crds.install }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {{- if .Values.crds.keep }}
    helm.sh/resource-policy: keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.21.0
  name: verdaclusters.infrastructure.cluster.x-k8s.io
  labels:
    cluster.x-k8s.io/v1beta2: v1beta1
    app.kubernetes.io/name: cluster-api-provider-verda
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: VerdaCluster
    listKind: VerdaClusterList
    plural: verdaclusters
    shortNames:
    - vcl
    singular: verdacluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster to which this VerdaCluster belongs
      jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - description: Cluster infrastructure is provisioned
      jsonPath: .status.initialization.provisioned
      name: Provisioned
      type: boolean
    - description: API endpoint
      jsonPath: .spec.controlPlaneEndpoint.host
      name: Endpoint
      type: string
    - description: Load balancer instance
      jsonPath: .status.loadBalancer.instanceID
      name: LB
      priority: 1
      type: string
    - description: Load balancer backends
      jsonPath: .status.loadBalancer.backends
      name: Backends
      priority: 1
      type: string
    - description: Service load balancer address
      jsonPath: .status.serviceLoadBalancer.address
      name: ServiceLB
      priority: 1
      type: string
    - description: Time duration since creation of VerdaCluster
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VerdaCluster is the Schema for the verdaclusters API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object.

              Servers should convert recognized schemas to the latest internal value, and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents.

              Servers may infer this from the endpoint the client submits requests to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: spec is the desired state of VerdaCluster.
            properties:
              controlPlaneEndpoint:
                description: 'controlPlaneEndpoint represents the endpoint used to communicate with the control plane.


                  Verda does not provide a managed load balancer. Either enable

                  controlPlaneLoadBalancer, in which case the provider fills this in with

                  the address of the load balancer instance it creates, or supply it: a DNS

                  name pointing at the control plane machine(s), or the address of a load

                  balancer managed outside of Cluster API.'
                minProperties: 1
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    minLength: 1
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              controlPlaneLoadBalancer:
                description: 'controlPlaneLoadBalancer configures a provider-managed load balancer in

                  front of the control plane: a Verda instance running haproxy in TCP mode

                  whose backends are kept in sync with the control plane machines.'
                minProperties: 1
                properties:
                  enabled:
                    description: enabled turns on the provider-managed load balancer.
                    type: boolean
                  image:
                    description: 'image is the Verda image the load balancer boots from. It must be a

                      Debian-based image with apt access. Defaults to 24.04.base (Ubuntu 24.04).'
                    maxLength: 256
                    type: string
                  instanceType:
                    description: instanceType is the Verda instance type of the load balancer. Defaults to CPU.4V.16G.
                    maxLength: 64
                    type: string
                  sshKeyIDs:
                    description: 'sshKeyIDs are additional Verda SSH keys to inject into the load balancer

                      instance, for troubleshooting. The provider always injects its own key.'
                    items:
                      maxLength: 128
                      minLength: 1
                      type: string
                    maxItems: 16
                    type: array
                    x-kubernetes-list-type: set
                type: object
              identityRef:
                description: 'identityRef references a Secret in the same namespace holding the Verda

                  API credentials for this cluster, with keys client-id, client-secret and

                  optionally base-url. When unset the credentials the manager was started

                  with are used.'
                properties:
                  name:
                    description: name is the name of the Secret.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              location:
                description: 'location is the Verda location code (e.g. FIN-01, FIN-02, FIN-03) in which

                  all machines of the cluster are created.'
                maxLength: 64
                minLength: 1
                type: string
              serviceLoadBalancer:
                description: 'serviceLoadBalancer configures a provider-managed load balancer for

                  Services of type LoadBalancer: a Verda instance running envoy whose

                  listeners are managed by the cloud controller manager in the workload

                  cluster. TCP and UDP are forwarded as-is (TLS passes through).'
                minProperties: 1
                properties:
                  enabled:
                    description: enabled turns on the provider-managed load balancer.
                    type: boolean
                  image:
                    description: 'image is the Verda image the load balancer boots from. It must be a

                      Debian-based image with apt access. Defaults to 24.04.base (Ubuntu 24.04).'
                    maxLength: 256
                    type: string
                  instanceType:
                    description: instanceType is the Verda instance type of the load balancer. Defaults to CPU.4V.16G.
                    maxLength: 64
                    type: string
                  sshKeyIDs:
                    description: 'sshKeyIDs are additional Verda SSH keys to inject into the load balancer

                      instance, for troubleshooting. The provider always injects its own key.'
                    items:
                      maxLength: 128
                      minLength: 1
                      type: string
                    maxItems: 16
                    type: array
                    x-kubernetes-list-type: set
                type: object
            required:
            - location
            type: object
          status:
            description: status is the observed state of VerdaCluster.
            properties:
              conditions:
                description: 'conditions represents the observations of a VerdaCluster''s current state.

                  Known condition types are Ready, ControlPlaneEndpointReady, LoadBalancerReady, Paused.'
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition transitioned from one status to another.

                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating the reason for the condition''s last transition.

                        Producers of specific condition types may define expected values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failureDomains:
                description: 'failureDomains is a list of failure domain objects synced from the infrastructure provider.

                  A VerdaCluster lives in a single location, which is its only failure domain.'
                items:
                  description: 'FailureDomain is the Schema for Cluster API failure domains.

                    It allows controllers to understand how many failure domains a cluster can optionally span across.'
                  properties:
                    attributes:
                      additionalProperties:
                        type: string
                      description: attributes is a free form map of attributes an infrastructure provider might use or require.
                      type: object
                    controlPlane:
                      description: controlPlane determines if this failure domain is suitable for use by control plane machines.
                      type: boolean
                    name:
                      description: name is the name of the failure domain.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              initialization:
                description: 'initialization provides observations of the VerdaCluster initialization process.

                  NOTE: Fields in this struct are part of the Cluster API contract and are used to orchestrate initial Cluster provisioning.'
                minProperties: 1
                properties:
                  provisioned:
                    description: 'provisioned is true when the infrastructure provider reports that the Cluster''s infrastructure is fully provisioned.

                      NOTE: this field is part of the Cluster API contract, and it is used to orchestrate initial Cluster provisioning.'
                    type: boolean
                type: object
              loadBalancer:
                description: loadBalancer holds the state of the provider-managed control plane load balancer.
                minProperties: 1
                properties:
                  address:
                    description: address is the public IP of the load balancer instance.
                    maxLength: 64
                    type: string
                  backends:
                    description: 'backends are the control plane addresses currently configured in haproxy.

                      Unused for the service load balancer, whose listeners live in the workload cluster.'
                    items:
                      maxLength: 64
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: set
                  instanceID:
                    description: instanceID is the identifier of the Verda instance running haproxy.
                    maxLength: 128
                    type: string
                  secretPublished:
                    description: 'secretPublished is true once the address and SSH keys have been written

                      to the workload cluster (service load balancer only).'
                    type: boolean
                  startupScriptID:
                    description: startupScriptID is the identifier of the load balancer's startup script.
                    maxLength: 128
                    type: string
                type: object
              serviceLoadBalancer:
                description: serviceLoadBalancer holds the state of the provider-managed service load balancer.
                minProperties: 1
                properties:
                  address:
                    description: address is the public IP of the load balancer instance.
                    maxLength: 64
                    type: string
                  backends:
                    description: 'backends are the control plane addresses currently configured in haproxy.

                      Unused for the service load balancer, whose listeners live in the workload cluster.'
                    items:
                      maxLength: 64
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: set
                  instanceID:
                    description: instanceID is the identifier of the Verda instance running haproxy.
                    maxLength: 128
                    type: string
                  secretPublished:
                    description: 'secretPublished is true once the address and SSH keys have been written

                      to the workload cluster (service load balancer only).'
                    type: boolean
                  startupScriptID:
                    description: startupScriptID is the identifier of the load balancer's startup script.
                    maxLength: 128
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
