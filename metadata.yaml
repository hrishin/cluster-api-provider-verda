# metadata.yaml maps provider release series to the Cluster API contract they
# implement. clusterctl reads it from the release assets.
apiVersion: clusterctl.cluster.x-k8s.io/v1alpha3
kind: Metadata
releaseSeries:
  - major: 0
    minor: 1
    contract: v1beta2
